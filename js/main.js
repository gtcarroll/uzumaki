const projects = [
    "Uzumaki"
];

window.onload = function() {
    window.onkeydown = function(e) { 
        return !(e.keyCode == 32);
    };
    //let projectParser = new ProjectParser(projects);
} 